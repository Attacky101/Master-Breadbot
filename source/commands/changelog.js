module.exports = {
    name: 'changelog',
    aliases: ['changes'],
    description: 'Shows the last three entries in the changelog.',
    execute(message, args) {
            // let last3messages
            // const messagechannel = message.guild.channels.cache.find(channel => channel.id === "739870465165623383")
            // const Discord = require('discord.js');
            // messagechannel.messages.fetch({limit: 3}).then(messages => messages.each((m) => {last3messages = last3messages + m.content
            // var embed = new Discord.MessageEmbed
            // embed = embed.setTitle("Changelog")
            // embed = embed.setDescription(last3messages)
            // message.channel.send(embed)
            // }))
            message.channel.send("no")
    },
};
